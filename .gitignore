# dependencies
/node_modules

# next.js
/.next/
/out/

# production
/build

# env
.env
.env*.local
# `npm run setup` backs up an existing .env here — same secrets, so never commit it.
.env.bak

# test coverage output
/coverage

# vitest JSON report used by the CI isolation gate
isolation-report.json

# misc
.DS_Store
*.pem
npm-debug.log*

# typescript
*.tsbuildinfo
next-env.d.ts
